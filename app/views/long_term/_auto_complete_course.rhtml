<ul class="course_list">
<% for course in @courses -%>
    <li class="autocomplete_course"><%= course.code %> <%= ( course.has_attribute?('number') ? course.number : '' ) %><% if course.has_attribute?('title') %> <br/> <em><%= course.title %></em><% end %></li>
<% end -%>
</ul>